<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Plain platform theme: no ActionBar, solid black window. Compose draws the rest. -->
    <style name="Theme.FlipperLauncher" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/fz_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
